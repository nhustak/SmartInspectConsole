<Window x:Class="SmartInspectConsole.Views.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Message with optional icon -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock x:Name="IconText" FontSize="32" Margin="0,0,16,0" VerticalAlignment="Top"/>
            <TextBlock x:Name="MessageText" Text="{Binding Message}" TextWrapping="Wrap"
                       VerticalAlignment="Center" MaxWidth="320"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="YesButton" Content="Yes" Width="80" Margin="0,0,8,0"
                    Click="YesButton_Click" Visibility="Collapsed"/>
            <Button x:Name="NoButton" Content="No" Width="80" Margin="0,0,8,0"
                    Click="NoButton_Click" Visibility="Collapsed"/>
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="0,0,8,0"
                    Click="OkButton_Click" Visibility="Collapsed"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80"
                    Click="CancelButton_Click" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>
